name: fotowah_cmf
description: "CMF System App for Fotowat Al Aqsa"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  very_good_analysis: ^10.0.0

flutter:
  uses-material-design: true